    #include <stdio.h>
    #include <stdlib.h>

    int main() {

    int x, i, j, ntermos; fat=1;
    long int fatorialatual;
    float soma =1.0 ;

    printf("Digite o valor de x:");
    scanf("%d",&x);

    printf("\nDigite o numero de termos:");
    scanf("%d",&ntermos);


     for (i = 1; i <= ntermos; i++) {
         fatorialatual = 1
         int numfatorial = i+2;


                for (j = 1; j <= numfatorial; j++) {

                    fatorialatual *= j;

        }

            int expoente = i + 1;
            float potenciax = 1.0
            int k;
            for (k = 1; k <= expoente; k++) {
            potenciax *= x; // Multiplica por x a cada vez
        }


            }


    }
